import {
    StyleSheet
} from 'react-native';

const styles = StyleSheet.create({
    container: {
        flex: 1
    },
    title: {
        fontSize: 30,
        fontFamily: 'Tahoma',
        textAlign: 'center',
        color: 'black',
        backgroundColor: 'aqua',
        padding: 5
    }
});

export default styles;